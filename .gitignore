# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Build
build/
builddir/
dist/

# Docs site build output (zensical)
/site/

# Tooling caches and reports
.reports/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*

# Mutation testing (mutmut sandbox copy + stats; run locally only)
mutants/
.mutmut-cache

# Atheris / libFuzzer crash reproducers + artifacts (dev-only fuzzing, #96)
crash-*
leak-*
timeout-*
oom-*
slow-unit-*
